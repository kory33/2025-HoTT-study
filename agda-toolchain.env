# Shared Agda toolchain versions for devcontainer and CI.
# (There's no standard tool to automatically install these Agda versions
#  such as rustup or lake, so this file only serves as a SSoT
#  for the versions to be used in both devcontainer and CI.)
AGDA_VERSION=2.8.0
AGDA_STDLIB_VERSION=2.3
